
// src/services/types.ts

/**
 * Represents coordinates in WGS 84 format.
 * An array of [longitude, latitude] number tuples.
 * Example: [[27.915, 47.775], [27.920, 47.778], ...]
 */
export type WGS84Coordinates = number[][];

// Add other shared types here as needed
